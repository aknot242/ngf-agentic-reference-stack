apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: keycloak-gateway
  namespace: keycloak
spec:
  gatewayClassName: nginx
  listeners:
    - name: keycloak
      port: 8082
      protocol: HTTP
